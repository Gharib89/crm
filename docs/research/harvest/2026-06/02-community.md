# Harvest 02 — Community pain points

**Family:** Practitioner pain mining — Stack Overflow, Reddit, Dynamics Community forums, practitioner blogs. High vote/comment counts = frequency/pain signals; rows capture the underlying TASK, not the specific bug.
**Collected:** 2026-06-10 by research subagent (read-only web research).

| Scenario | Persona | Frequency-signal | Pain-signal | Tooling-today | Source URL |
|---|---|---|---|---|---|
| Register, update, and step-configure plugin assemblies (both) | pro-code | high — every plugin change requires re-registration; daily in active dev cycles | high — manual PRT clicks per update; version mismatch kills solution import; step forgotten in solution = silent promotion gap | Plugin Registration Tool (PRT) or `pac plugin push`; XrmToolBox; Azure DevOps Build Tools for CI/CD | https://www.solvingdynamics365.com/guides/plug-in-registration-tool-deep-dive |
| Debug plugin failures via Plugin Trace Log (both) | pro-code | high — synchronous 2-min timeout, infinite-loop depth-8, security-context errors are constant; every implementation hits multiple of the top-5 errors | high — trace log is fire-and-forget, not correlated; production observability requires App Insights wiring most teams skip | Plugin Trace Log in UI; Plugin Profiler in PRT; App Insights (advanced); FakeXrmEasy for unit tests | https://thedataversedev.com/blog/common-plugin-errors-dataverse/ |
| Promote solution through dev→test→prod (both) | both | high — every release requires it; manual promotion is "DevOps by spreadsheet" without pipelines | high — missing dependency errors in ~45–50% of manual deploys; schema breakage on column-type changes; unmanaged layer drift silently blocks managed updates | Manual ZIP export/import; Power Platform Build Tools; `pac solution pack/unpack`; Pipelines for Power Platform | https://www.solvingdynamics365.com/guides/solution-import-export-pipelines |
| Diagnose and remove unmanaged layer that blocks managed solution update (both) | both | high — any "quick fix in prod" or direct-portal edit creates a blocking layer; happens in every long-lived org | high — change appears deployed but doesn't render; root cause not obvious until "Solution Layers" view; no automated detection in most teams | Solution Layers view in maker portal; XrmToolBox Solution Layers Explorer; `pac` with `OverwriteUnmanagedCustomizations` flag | https://dev.to/sapotacorp/solution-layering-in-dataverse-why-managed-changes-dont-appear-bmj |
| Write and debug JavaScript form scripts with Client API (both) | pro-code | high — every meaningful form customization beyond business rules needs JS; multi-event handlers are ubiquitous | med — browser DevTools workflow is workable but every change requires publish + cache-clear cycle; async/await race conditions and deprecated API usage are recurring bugs | Browser DevTools + `debugger;`; Fiddler AutoResponder for local testing; TypeScript + webpack/esbuild build step | https://dev.to/nikhildynamicsce/debugging-javascript-in-dynamics-365-ce-forms-ribbons-and-pcfs-10g9 |
| Publish web resource changes and clear browser cache for testing (both) | pro-code | high — required after every JS/HTML/CSS change during development; multiple publish-clear cycles per hour | med — no hot-reload; publish latency + cache-clear ritual adds friction; large teams hit "who published last?" conflicts | "Publish All Customizations" in maker portal; `pac` solution push; browser DevTools cache purge | https://dev.to/kkazala/client-script-in-model-driven-apps-do-it-right-from-the-start-19c5 |
| Compose complex FetchXML queries (aggregates, N:N joins, outer joins) (both) | pro-code | high — every integration, view, and plugin server query uses FetchXML or OData; aggregate and link-entity gaps push teams to FetchXML | med — hand-writing XML is error-prone; 5,000-row page limit silently truncates if paging cookie not implemented; aggregate cap of 50,000 rows surprises teams | FetchXML Builder in XrmToolBox; Advanced Find → Edit Columns → Download FetchXML; `pac` modelbuilder | https://www.xrmtoolbox.com/plugins/Cinteros.Xrm.FetchXmlBuilder/ |
| Manage OAuth/Entra app registration for external integrations (online) | pro-code | high — every integration, reporting tool, or external API consumer needs app registration; auth errors are top Stack Overflow question cluster | high — AADSTS errors, wrong scope format, missing app user in environment, and client-secret vs certificate confusion are repeated across hundreds of SO questions | Azure portal app registrations; `pac auth create`; Postman for token testing | https://stackoverflow.com/questions/71968454/dynamics-365-oauth-2-0-authentication-failed |
| Configure NTLM / IFD auth for on-premises integrations (on-prem only) | pro-code | med — every on-prem integration must use NTLM or OAuth via ADFS; Dataverse Service Client dropped WS-Trust in .NET 6+ | high — no official first-party .NET 6+ NTLM path; community workarounds required; ADFS version matrix (2016 vs 2019) determines feasibility | `CrmServiceClient` with legacy SDK for .NET Framework; community DVSC fork for .NET 6+ on-prem; Fiddler tracing | https://github.com/janis-veinbergs/DataverseServiceClientOnPremSamples |
| Handle API throttling (HTTP 429) in high-volume integrations (online) | pro-code | med — 6,000 req/user/5 min limit hits any bulk operation or integration running against a single app user | high — silent data loss if code ignores `Retry-After`; `ExecuteMultiple` batching reduces count but is non-obvious; Power Automate flows share the same quota as their connection user | `ExecuteMultiple` (up to 1,000 ops/request); multiple app users; `Retry-After` header handling; Dataverse Search for read-heavy reads | https://crmcurator.com/articles/dynamics365/dataverse-quotas-and-throttling/ |
| Migrate classic background workflows to Power Automate (both) | both | high — deprecation pressure is active (classic designer read-only in 2026 wave 1); every org has dozens to hundreds of classic workflows | high — 30–50% of orgs have inactive duplicates inflating scope; synchronous classic workflows lose transaction semantics in async flows; custom workflow activities have no direct equivalent | Microsoft migration tooling (generates draft flow); WorkflowModerniser OSS tool; manual rebuild; parallel-run validation | https://crmcurator.com/articles/dynamics365/workflow-to-modern-flow-migration/ |
| Bulk-import or migrate records while preserving audit fields (createdby, createdon) (both) | both | med — data migrations and environment refreshes are project milestones; preserving history is a near-universal requirement | high — `createdon`/`modifiedon`/`createdby`/`modifiedby` are locked system fields; only `overriddencreatedon` is OOB-writable; full audit preservation requires a custom plugin interceptor | KingswaySoft SSIS; Azure Data Factory; custom C# console app + plugin for audit override; Dataverse Migrator (dvmig) OSS tool | https://stackoverflow.com/questions/79358965/how-do-you-preserve-createdon-createdby-modifiedon-and-modifiedby-in-a-datave |
| Build and maintain CI/CD pipeline for solution + plugin deployment (both) | pro-code | med — enterprise teams treat this as mandatory; SMB teams often still manual; growing fast | high — solution is metadata-heavy, not code-only; environment-specific connection references, environment variables, and app users must be pre-created; no single canonical pattern | Azure DevOps + Power Platform Build Tools; GitHub Actions + `pac` CLI; Deployment Pipelines for Power Platform (online-only) | https://medium.com/@asyncme/ci-cd-for-microsoft-dynamics-365-ce-a-complete-modern-enterprise-grade-guide-72a8e21e8a55 |
| Export and version security roles correctly across environments (both) | customizer | med — every project defines custom security roles; security roles must travel in solutions; manually re-creating in each env is error-prone | med — importing a security role from a managed solution strips manually-added privileges; must use the same solution for all role updates or partial privileges are silently lost; BPF privileges (`prvAppendToWorkflow`) commonly missed | Include security role in solution; export managed solution; import to target; never re-create manually | https://github.com/MicrosoftDocs/power-platform/blob/main/power-platform/alm/how-managed-solutions-merged.md |
| Customize ribbon/command bar buttons and rules (both) | pro-code | med — ribbon customization is required for every model-driven app with custom actions; on-prem has no Command Designer (online-only) | med — on-prem requires hand-editing RibbonDiffXml inside solution ZIP; online Command Designer removes the XML burden but is online-only; library reference missing on one form silently breaks the button | Ribbon Workbench (XrmToolBox) for both targets; Command Designer (online-only); manual solution XML on-prem | https://www.xrmtoolbox.com/plugins/RibbonWorkbench2016/ |
| Investigate and resolve solution import failures (missing dependencies, schema mismatches) (both) | both | high — every promotion cycle risks import failure; manual deploys fail in ~45% of first attempts | med — error messages surface GUIDs, not component names; must open customizations.xml to trace hidden form-to-view dependencies; column-type change is permanently unsupported (delete + recreate required) | Solution import log download; customizations.xml inspection; XrmToolBox Solution Checker; `pac solution check` | https://nishantrana.me/2026/02/24/solution-failed-to-import-missing-lookup-view-dependency-in-dataverse-dynamics-365/ |
| Design and tune FetchXML/OData queries to avoid 30-second timeouts (both) | pro-code | med — large orgs with millions of rows hit query timeouts on unindexed columns; common in reporting and integrations | high — non-indexed custom string columns force full-table scans; `$expand` chain > 2 levels materializes in-memory before paging; FetchXML aggregate cap at 50,000 rows; fix is add index in maker portal, not obvious | FetchXML Builder; Dataverse Performance Insights; manual index creation via maker portal Settings → Performance | https://dev.to/sapotacorp/dataverse-web-api-fetchxml-vs-odata-patterns-that-dont-time-out-2e6p |
| Manage form load performance with too many JS web resources (both) | pro-code | med — every project accumulates JS debt; form load is user-visible and drives ticket volume | med — 20 separate web resource HTTP round trips on OnLoad; synchronous XHR blocks paint; no built-in bundling; dev envs hide the problem (small datasets) | `webpack`/`esbuild` bundling; Solution Checker; browser Performance tab Long Tasks profiling; async/await refactor | https://crmcurator.com/articles/dynamics365/form-scripting-performance/ |
| Migrate on-premises CE environment to Dataverse online (on-prem→online) | pro-code | low — event-driven (mainstream support ended); large backlog of deferred migrations now being forced | high — full-trust plugins must be audited for sandbox violations (file system, local SQL, arbitrary network); SSRS reports ~30–50% won't survive; NTLM auth must be replaced with Entra OAuth; dialogs must be rebuilt | Microsoft Customer Engagement Migration Tool (metadata + data); plugin audit + recompile; Power BI / Paginated Reports for SSRS | https://crmcurator.com/articles/dynamics365/onprem-to-online-migration-2026/ |
| Inventory, list, and diff workflows/processes across environments (both) | both | med — every governance review, migration, or handover project needs a full process inventory; no native cross-env diff | med — no OOB "compare workflows between dev and prod" tool; duplicate detection requires querying `workflow` entity filtered to type=1 definitions; inactive duplicates inflate scope | XrmToolBox plugins (Workflow Manager, Bulk Workflow Manager); FetchXML against `workflow` table; custom PAC CLI scripts | https://crmcurator.com/articles/dynamics365/workflow-to-modern-flow-migration/ |

## Method

Queries used:

1. `mcp__claude_ai_Exa__web_search_exa`: "Dynamics 365 CE developer recurring pain points plugin development deployment workflow 2024 2025"
2. `mcp__claude_ai_Exa__web_search_exa`: "Stack Overflow most voted questions dynamics-crm dynamics-365 dataverse developer problems"
3. `mcp__claude_ai_Exa__web_search_exa`: "Reddit r/Dynamics365 common pain points customizer consultant recurring problems solutions imports exports"
4. `mcp__claude_ai_Exa__web_search_exa`: "Dynamics 365 CE customizer consultant pain points solution import export security roles forms views BPF recurring problems"
5. `WebSearch`: "dynamics 365 CRM developer pain points fetchxml odata solution import ALM environment promotion problems 2024 2025"
6. `WebSearch`: "dynamics 365 customizer consultant common problems solution layers managed unmanaged security role export import 2024"
7. `mcp__claude_ai_Exa__web_search_exa`: "Dynamics 365 JavaScript web resource development debugging console errors form scripting pain points practitioners"
8. `mcp__claude_ai_Exa__web_search_exa`: "Dynamics 365 data migration bulk import records preserve audit history environment refresh common issues 2024"
9. `WebSearch`: "Dynamics 365 XrmToolBox most used tools developers consultants FetchXML builder workflow solution 2024 2025"
10. `mcp__claude_ai_Exa__web_search_exa`: "Dynamics 365 on-premises vs online developer differences NTLM OAuth plugin registration tooling gaps 2024 2025"
11. `mcp__claude_ai_Exa__web_search_exa`: "Dynamics 365 CE FetchXML aggregate query limitations OData API throttling developer workarounds"
12. `mcp__claude_ai_Exa__web_search_exa`: "Dynamics 365 CE workflow classic vs Power Automate migration deprecated conversion developer pain 2024 2025"
13. `WebFetch` against Stack Overflow `dynamics-crm` tag (blocked), Reddit `r/Dynamics365` (blocked) — frequency signals inferred from SO question titles, vote patterns, and community blog post clusters instead.

Sources drawn from: practitioner blogs (crmcurator.com, solvingdynamics365.com, thedataversedev.com, theaugmenteddev.com, dev.to), Microsoft Learn official docs, Dynamics Community forums, Stack Overflow question patterns, and GitHub community tools (dvmig, workflow-moderniser, XrmToolBox plugin catalog).
