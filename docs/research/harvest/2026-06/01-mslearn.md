# Harvest 01 — MS Learn / official docs

**Family:** Microsoft Learn / official documentation (sanctioned recurring tasks — each documented how-to/tutorial topic = evidence).
**Collected:** 2026-06-10 by research subagent (read-only web research).

| Scenario | Persona | Frequency-signal | Pain-signal | Tooling-today | Source URL |
|---|---|---|---|---|---|
| Write, compile, and register a C# plug-in assembly and step (both) | pro-code | **high** — every feature that needs server-side logic creates a new plug-in or step; entire Developer Guide is structured around it | **high** — no in-process debugger on online; trace logs require enabling per-step; on-prem supports profiler but it adds friction | Visual Studio + Plug-in Registration Tool (PRT) or Power Platform Tools VS extension; NuGet `Microsoft.PowerApps.CLI` | https://learn.microsoft.com/power-apps/developer/data-platform/register-plug-in |
| Debug a failing plug-in via trace logs (both) | pro-code | **high** — every non-trivial plug-in fix starts here; Plugin Trace Viewer XrmToolBox plugin has 90k+ lifetime downloads | **high** — tracing must be enabled at step level; default log retention is short; no live debugger on online; filtering native UI is inadequate at volume | ITracingService + Plug-in Registration Tool; XrmToolBox Plugin Trace Viewer plugin | https://learn.microsoft.com/power-apps/developer/data-platform/tutorial-write-plug-in#view-trace-logs |
| Write JavaScript form-event handlers and upload as a JS web resource (both) | pro-code | **high** — the most common client-side extension mechanism; every non-trivial form has at least one `OnLoad`/`OnSave`/`OnChange` handler | **med** — no compile-time type checking in browser; upload-then-publish cycle is slow; async gotchas (sync XHR deprecated) | VS Code / Visual Studio → upload via maker portal or PAC CLI; Fiddler AutoResponder for local dev loop | https://learn.microsoft.com/power-apps/developer/model-driven-apps/client-scripting |
| Build and test a FetchXML query and embed it in code or a view (both) | pro-code | **high** — every aggregate report, advanced list view, plug-in data retrieval, and integration query uses FetchXML or OData; community tools show massive adoption | **med** — syntax is verbose XML; no intellisense in production environments; `link-entity` join semantics differ from SQL; `top` vs paging interplay | FetchXML Builder in XrmToolBox; Power Platform Toolbox FetchXML Studio; `pac env fetch`; hand-authored XML | https://learn.microsoft.com/power-apps/developer/data-platform/fetchxml/overview |
| Export solution, unpack to source control, pack, and import to another environment (both) | pro-code | **high** — required on every deployment cycle; the entire ALM model is solution-based; both manual and CI/CD paths hit this | **high** — XML diffs are enormous and unreadable; managed vs unmanaged layer confusion causes silent overwrites; solution import failures give cryptic errors; YAML format only ≥CLI 2.4.1 | PAC CLI `pac solution export/import/clone/pack/unpack`; Power Platform GitHub Actions; Azure DevOps tasks; Solution Packager | https://learn.microsoft.com/power-platform/alm/use-source-control-solution-files |
| Create or edit a custom table (entity) with columns and relationships (both) | customizer | **high** — foundational to every implementation; MS Learn guided project centers entirely on this task | **low** — maker portal wizard is straightforward; main friction is naming discipline (prefix, logical name immutable after save) | Power Apps maker portal table designer; PAC CLI `pac solution clone` for ALM | https://learn.microsoft.com/power-apps/maker/data-platform/fields-overview |
| Design and publish a main form (tabs, sections, columns, sub-grids) (both) | customizer | **high** — every new table or requirement change touches form layout; publish-all is required before users see changes | **med** — publish step is easy to forget; form editor in classic designer is slow; no offline authoring (online-only for cloud; on-prem you lose work if connectivity drops) | Power Apps modern form designer; classic solution explorer form editor (on-prem) | https://learn.microsoft.com/dynamics365/customerengagement/on-premises/customize/create-edit-main-forms?view=op-9-1 |
| Configure view columns, filters, and sort order for a table (both) | customizer | **high** — virtually every implementation customizes system views or creates personal/system views per business unit | **low** — drag-and-drop view designer is mature; related-table column lookup occasionally slow; Advanced Find (online-only, web client) for ad-hoc | Power Apps view designer; Classic view editor (on-prem) | https://learn.microsoft.com/power-apps/maker/model-driven-apps/choose-and-configure-columns |
| Create or copy and configure security roles (record- and field-level) (both) | customizer | **high** — every new user group or table requires role changes; field-level security profiles are a separate object requiring separate assignment | **high** — privilege grid has 200+ rows × 8 access levels; copying a built-in role and trimming is the only sane path; privilege accumulation model means you cannot restrict once granted broadly; on-prem and online behave identically | Power Platform admin center security role editor; classic security role form in solution explorer | https://learn.microsoft.com/power-platform/admin/database-security |
| Design and activate a Business Process Flow with stages and steps (both) | customizer | **high** — Lead-to-Opportunity and Case BPFs are standard; virtually every Sales/Service implementation adds or customises at least one | **med** — BPF instances are hidden Dataverse rows that grow unboundedly; activating a new version does not migrate in-flight instances; stage-gating bypassed by Web API/bulk import; creation now only via solution explorer (Power Automate UI deprecated for BPF since Aug 2022) | Power Apps solution explorer BPF designer; Power Automate (create only, legacy); code via `RetrieveActivePath` message | https://learn.microsoft.com/power-automate/create-business-process-flow |
| Customise command bar / ribbon buttons with enable rules and JavaScript actions (on-prem + online) | pro-code | **med** — needed whenever a business wants contextual toolbar actions (e.g. custom close, clone, status-change buttons); ribbon team blog and XrmToolBox Ribbon Workbench are widely cited | **high** — classic ribbon XML is verbose and poorly diff-able; Command Designer (online-only) is easier but limited; HideCustomAction cannot be removed without a new solution version; UCI grid selection rules differ from classic | Classic: RibbonDiff XML via solution export; online: Command Designer in Power Apps; XrmToolBox Ribbon Workbench | https://learn.microsoft.com/power-apps/developer/model-driven-apps/define-ribbon-enable-rules |
| Integrate external systems via Dataverse Web API (OData CRUD + batch) (both) | pro-code | **high** — every enterprise D365 deployment integrates with ERP, HR, or ticketing systems; Web API is the canonical REST interface for both on-prem and online | **med** — NTLM/OAuth token management differs; `@odata.bind` navigation-property casing must match `$metadata`; batch requests add complexity; no SDK for non-.NET languages | HTTP client in any language; Python/Node/JS against OData v4; `Microsoft.Xrm.Sdk` (SDK for .NET) | https://learn.microsoft.com/power-apps/developer/data-platform/webapi/overview |
| Trigger outbound integration from plug-in (webhook, Azure Service Bus, or HTTP call) (both) | pro-code | **med** — common in event-driven architectures (notify ERP on Opportunity close, post to Teams, enqueue to Service Bus); MS TechTalk docs dedicate a full article to this pattern | **med** — service endpoint registration is separate from plug-in step registration; sandbox plug-ins on online restrict outbound DNS; on-prem allows unrestricted but requires network access | PRT service endpoint registration; Azure Service Bus / Event Grid; webhook HTTP POST; Power Automate HTTP trigger as receiver | https://learn.microsoft.com/dynamics365/guidance/techtalks/integrate-finance-operations-dataverse#outbound-integration---exporting-data-from-dataverse |
| Generate early-bound entity classes for plug-in development (both) | pro-code | **med** — standard practice on typed C# plug-in projects; Early Bound Generator V2 in XrmToolBox is one of the most downloaded plugins | **med** — regeneration required after every schema change; large orgs produce multi-MB files; option-set enums go stale silently | XrmToolBox Early Bound Generator V2; `pac modelbuilder build`; CrmSvcUtil (legacy) | https://learn.microsoft.com/power-apps/developer/data-platform/org-service/generate-early-bound-classes |
| Configure field-level (column-level) security profiles and assign to users/teams (both) | customizer | **med** — needed for salary, SSN, or sensitive financial fields; separate profile objects exist parallel to security roles | **med** — two separate objects to manage (field permission + profile assignment); field security applies even in API calls, so plug-in impersonation can break reads | Power Platform admin center; classic field security profile editor in solution explorer | https://learn.microsoft.com/power-platform/admin/field-level-security |
| Build and run Power Automate flows for D365 automation (scheduled, record-triggered) (online — on-prem relies on classic workflows) | both | **high** — nearly every D365 project uses at least one flow for notifications, approvals, or cross-system sync; MS Learn dedicated training module | **med** — flows are not source-controlled natively (graphical canvas, hard to diff/review); UTC vs local-date filter bugs are a repeat community issue; API throttling at 6 000 Dataverse calls/5 min; ownership/service-principal governance | Power Automate designer; solution-aware flows via PAC CLI for export; Azure DevOps / GitHub Actions for ALM | https://learn.microsoft.com/power-automate/overview-cloud |
| Export and import data between environments using data migration tools (both) | customizer | **med** — common at go-live, UAT seeding, and post-upgrade validation; referenced in ALM basics and functional consultant learning path | **med** — GUIDs differ across orgs breaking lookups; managed solution import removes data in custom tables if the solution is deleted; no built-in diff | Configuration Migration Tool; `pac data export/import`; Power Platform Dataflows; third-party tools (KingswaySoft, Scribe) | https://learn.microsoft.com/power-platform/alm/basics-alm |
| Set up and iterate on a CI/CD pipeline for solution deployment (Azure DevOps / GitHub) (both) | pro-code | **med** — growing adoption as teams professionalise ALM; Power Platform GitHub Actions and Azure DevOps tasks are first-party | **high** — solution XML diffs are large; environment URL and service-principal secrets management is manual; unmanaged layer in target env silently overrides managed; YAML source format requires CLI ≥2.4.1 | Power Platform GitHub Actions; Azure DevOps Power Platform tasks; PAC CLI in pipeline; SolutionPackager | https://learn.microsoft.com/power-platform/alm/devops-github-available-actions |

## Method

Queries used:

1. `microsoft_docs_search`: "Dynamics 365 CE plugin development recurring tasks tutorial" · "Dataverse customize model-driven apps tables columns forms views" · "Dataverse FetchXML query developer guide" · "Power Platform ALM solution concepts managed unmanaged deployment" · "Dynamics 365 JavaScript web resources client scripting form events" · "Dataverse security roles field security profiles access control" · "plugin registration tool Dataverse step message entity" · "business process flow BPF stages steps create Dataverse" · "Dynamics 365 Dataverse Web API OData integration external system" · "Power Platform CLI pac solution export import CI CD source control" · "Dynamics 365 ribbon command bar customize button enable rule JavaScript"
2. `mcp__claude_ai_Exa__web_search_exa`: "Dynamics 365 CE developer daily workflow pain points recurring tasks community blog 2024 2025" · "Dynamics 365 functional consultant customizer common tasks forms views security roles business process flow pain points" · "Dynamics 365 CE on-prem IFD developer pain points plugin debugging trace logs XrmToolBox daily use 2024"
