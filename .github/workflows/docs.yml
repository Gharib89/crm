name: docs
on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - 'crm/**'
      - 'setup.py'
      - 'CHANGELOG.md'
      - '.github/workflows/docs.yml'
  pull_request:
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - 'crm/**'
      - 'setup.py'
      - 'CHANGELOG.md'
      - '.github/workflows/docs.yml'
permissions:
  contents: read
concurrency:
  group: docs-${{ github.ref }}
  # Cancel superseded PR runs; never cancel a push:main deploy.
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
jobs:
  # Single job: build --strict once (the gate), then deploy that same site/ on
  # push. Avoids the previous second job's redundant install + rebuild.
  docs:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      # No pip cache here: this job also deploys to Cloudflare Pages on push, and
      # a cache restored in that publish context is a cache-poisoning vector
      # (zizmor). Docs deps are small, so the cache saved little.
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - name: Install docs deps
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[docs]"
      - name: Build (strict)
        run: mkdocs build --strict
      - name: Deploy to Cloudflare Pages
        if: github.event_name == 'push'
        uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0  # v4.0.0
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: '4.97.0'
          command: pages deploy site --project-name=crm-cli-docs
