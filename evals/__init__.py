"""Behavioral skill-evaluation harness — excluded from the wheel (see setup.py).

Not collected by the default pytest suite (testpaths = crm/tests); run on demand
with `pytest evals/skill`. See evals/skill/README.md.
"""
